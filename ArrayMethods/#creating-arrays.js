// way 1 array literals (most common)

const numbers = [1, 2, 3, 4, 5, 6];
console.log(numbers);


// way 2 mixed type

const mixed = ["Rohit", 1, true, null];
console.log(mixed);


// way3 empty array

const empty = [];
console.log(empty);

// Way 4 Array of objects

const students = [
    { name: "Rohit", age: 33 },
    { name: "Rahul ", age: 22 },
];
console.log(students);

