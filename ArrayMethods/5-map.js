const numbers = [1, 2, 3, 4, 5, 6];
const double = numbers.map(num => num * 2)
console.log(double);


// realword example

const students = ["Raja", "Rahul", "sandeep", "amol"];
const uppercase = students.map(name => name.toUpperCase());
console.log(uppercase);
