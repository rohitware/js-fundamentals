const fruits = ['apple', 'banana', 'cherry'];

fruits.forEach(function (fruit) {
    console.log(fruit);
});



// new example with arrow function
const numbers = [1, 2, 3, 4, 5];

numbers.forEach(number => console.log(number * 2)); 